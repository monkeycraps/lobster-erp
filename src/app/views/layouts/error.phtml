<?php 
if( isset( $errors, $errors['success'] ) ){ ?>
	<div class='alert alert-success'>
		<?php if( isset( $errors['message'] ) ) { ?>
			<?= $error['message'] ?>
		<? }?>
	</div>
<? }else if( $errors ){ ?>
	<div class='alert alert-danger'>
		<?php foreach( $errors as $key=>$one ) { ?>
			<?= !is_numeric($key) ? "{$key}:{$one}" : $one ?>
		<? }?>
	</div>
<? }?>