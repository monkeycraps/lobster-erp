<?php
$fanchan_list = $controller->fanchan_list; 
foreach( $fanchan_list as $one ){?>
	<tr class='' data-category='<?php echo $one['category']?>' 
		data-product='<?php echo $one['product']?>' >
		<td><?=$one['category_name']?></td>
		<td><?=$one['product_name']?></td>
		<td><?= isset( $one['cnt'] ) ? $one['cnt'] : 0?></td>
		<td><?= isset( $one['cnt_new'] ) ? $one['cnt_new'] : 0?></td>
		<td class='btn-state' >
			<span class='btn btn-default btn-xs btn-fanchan'>全部返厂</span>
		</td>
	</tr>
<?php }?>